/**
 * Next.js adapter for route-level navigation outcomes.
 * Temporary migration boundary — TanStack Start will map these to its own
 * not-found and redirect helpers.
 */
export { notFound, redirect } from "next/navigation";
